proc logistic data=scored_train;
  freq trials;  /* because your "success/trials" is effectively trials replicated */
  model glgl_response(event='1') = glgl_response_model / nofit;
  ods select Association;
run;
