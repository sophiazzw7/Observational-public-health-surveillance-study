data rmse_calc;
  set scored_train;
  error_sq = (glgl_response - glgl_response_model)**2;
run;

proc means data=rmse_calc mean;
  var error_sq;
  output out=rmse_out mean=mean_sq_error;
run;

data rmse_out;
  set rmse_out;
  rmse = sqrt(mean_sq_error);
run;

proc print data=rmse_out;
run;
